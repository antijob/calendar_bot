name: deployment

on:
    push:
        branches:
            - main
        tags:
            - "v*"
    pull_request:
    workflow_dispatch:
        inputs:
            runType:
                description: "How to run this build?"
                required: true
                default: "default"
                type: choice
                options:
                    - Force build
                    - Default

jobs:
    verify:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.12"

            - name: Install dependencies
              run: pip install -r requirements.txt

            - name: Run verify.sh
              run: bash scripts/verify.sh

            - name: Run tests
              run: bash scripts/run-tests.sh

    check-docs:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Run check-docs.sh
              run: bash scripts/check-docs.sh

    build:
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        needs: [verify, check-docs]
        outputs:
            IMAGE_TAG: ${{ steps.build-image.outputs.IMAGE_TAG || steps.set-tag.outputs.IMAGE_TAG || 'main' }}
        runs-on: ubuntu-latest
        environment: ${{ github.ref == 'refs/heads/main' && 'Prod' || 'Unknown' }}
        steps:
            - uses: actions/checkout@v4

            - name: Set branch name and commit hash
              id: set-branch
              run: |
                  BRANCH_NAME=${GITHUB_REF#refs/heads/}
                  COMMIT_HASH=${GITHUB_SHA::7}
                  echo "BRANCH_NAME=${BRANCH_NAME}" >> $GITHUB_OUTPUT
                  echo "COMMIT_HASH=${COMMIT_HASH}" >> $GITHUB_OUTPUT

            - name: Configure Docker Hub auth credentials
              run: |
                  echo "$DOCKER_PUSH_TOKEN" | docker login --username "$DOCKER_USERNAME" --password-stdin
              env:
                  DOCKER_USERNAME: antijob
                  DOCKER_PUSH_TOKEN: ${{ secrets.DOCKER_PUSH_TOKEN }}

            - uses: dorny/paths-filter@v3
              id: filter
              with:
                  filters: |
                      app:
                        - 'calendar_bot.py'
                        - 'requirements.txt'
                        - 'Dockerfile'
                        - 'docker-compose.prod.yml'
                        - '.github/**'

            - name: Build image
              id: build-image
              if: ${{ steps.filter.outputs.app == 'true' || github.event.inputs.runType == 'Force build' }}
              run: |
                  BRANCH_NAME=${GITHUB_REF#refs/heads/}
                  COMMIT_HASH=${GITHUB_SHA::7}
                  IMAGE_TAG="${BRANCH_NAME}-${COMMIT_HASH}"

                  docker build . \
                    -t antijob/antijob-calendar:${IMAGE_TAG} \
                    -t antijob/antijob-calendar:${BRANCH_NAME} \
                    -t antijob/antijob-calendar:latest

                  docker push antijob/antijob-calendar:${IMAGE_TAG}
                  docker push antijob/antijob-calendar:${BRANCH_NAME}
                  docker push antijob/antijob-calendar:latest

                  echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_OUTPUT

            - name: Set fallback tag
              id: set-tag
              if: ${{ steps.build-image.outcome == 'skipped' }}
              run: echo "IMAGE_TAG=main" >> $GITHUB_OUTPUT

    deploy:
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        runs-on: ["self-hosted", "swarm_manager"]
        environment: ${{ github.ref == 'refs/heads/main' && 'Prod' || 'Unknown' }}
        needs: [build]
        env:
            IMAGE_TAG: ${{ needs.build.outputs.IMAGE_TAG }}
        steps:
            - uses: actions/checkout@v4

            - name: Docker login
              run: |
                  echo "$DOCKER_PULL_TOKEN" | docker login --username "$DOCKER_USERNAME" --password-stdin
              env:
                  DOCKER_USERNAME: antijob
                  DOCKER_PULL_TOKEN: ${{ secrets.DOCKER_PULL_TOKEN }}

            - name: Prepare swarm secrets
              env:
                  GOOGLE_API_KEY: "${{ secrets.GOOGLE_API_KEY }}"
                  TELEGRAM_BOT_TOKEN: "${{ secrets.TELEGRAM_BOT_TOKEN }}"
              run: |
                  set +x
                  GOOGLE_API_KEY_HASH="$(printf '%s' "${GOOGLE_API_KEY}" | sha256sum | cut -c1-12)"
                  TELEGRAM_BOT_TOKEN_HASH="$(printf '%s' "${TELEGRAM_BOT_TOKEN}" | sha256sum | cut -c1-12)"

                  GOOGLE_API_KEY_SECRET_NAME="secret.GOOGLE_API_KEY.${GOOGLE_API_KEY_HASH}"
                  TELEGRAM_BOT_TOKEN_SECRET_NAME="secret.TELEGRAM_BOT_TOKEN.${TELEGRAM_BOT_TOKEN_HASH}"

                  if ! docker secret inspect "${GOOGLE_API_KEY_SECRET_NAME}" >/dev/null 2>&1; then
                    printf '%s' "${GOOGLE_API_KEY}" | docker secret create "${GOOGLE_API_KEY_SECRET_NAME}" -
                    echo "Created ${GOOGLE_API_KEY_SECRET_NAME}"
                  else
                    echo "${GOOGLE_API_KEY_SECRET_NAME} already exists"
                  fi

                  if ! docker secret inspect "${TELEGRAM_BOT_TOKEN_SECRET_NAME}" >/dev/null 2>&1; then
                    printf '%s' "${TELEGRAM_BOT_TOKEN}" | docker secret create "${TELEGRAM_BOT_TOKEN_SECRET_NAME}" -
                    echo "Created ${TELEGRAM_BOT_TOKEN_SECRET_NAME}"
                  else
                    echo "${TELEGRAM_BOT_TOKEN_SECRET_NAME} already exists"
                  fi

                  {
                    echo "GOOGLE_API_KEY_SECRET_NAME=${GOOGLE_API_KEY_SECRET_NAME}"
                    echo "TELEGRAM_BOT_TOKEN_SECRET_NAME=${TELEGRAM_BOT_TOKEN_SECRET_NAME}"
                  } >> "$GITHUB_ENV"
                  set -x
            - name: Deploy stack
              env:
                  GOOGLE_API_KEY_SECRET_NAME: "${{ env.GOOGLE_API_KEY_SECRET_NAME }}"
                  TELEGRAM_BOT_TOKEN_SECRET_NAME: "${{ env.TELEGRAM_BOT_TOKEN_SECRET_NAME }}"
                  CALENDAR_ID: "${{ secrets.CALENDAR_ID }}"
                  TELEGRAM_GROUP_ID: "${{ secrets.TELEGRAM_GROUP_ID }}"
              run: |
                  DEPLOY_TAG="${IMAGE_TAG:-main}"
                  echo "Deploying antijob/antijob-calendar:${DEPLOY_TAG}"

                  docker pull antijob/antijob-calendar:${DEPLOY_TAG}

                  docker stack deploy \
                    --resolve-image always \
                    -c docker-compose.prod.yml \
                    --with-registry-auth \
                    antijob_calendar

                  docker service update --force \
                    --image antijob/antijob-calendar:${DEPLOY_TAG} \
                    antijob_calendar_scheduler
